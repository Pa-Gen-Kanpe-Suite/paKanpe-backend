name: Backend CD

on:
  push:
    branches: [main, QA/devOPS-branch]

permissions:
  contents: read
  packages: write

jobs:
  # --- Étape 1 : Exécution des tests avant la construction ---
  test:
    name: Run Pytest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f pyproject.toml ]; then pip install .; fi
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          pip install pytest pytest-asyncio pytest-cov httpx ruff alembic psycopg[binary]

      - name: Run Pytest Suite
        run: pytest
        env:
          DATABASE_URL: sqlite:///./test.db
          JWT_SECRET: cd-test-secret-with-more-than-thirty-two-characters
          NO_SHOW_GRACE_SECONDS: 300

  # --- Étape 2 : Build, Push Docker Image & Deploy ---
  build-and-deploy:
    name: Build Docker Image & Deploy
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Downcase repository name
        run: echo "REPO_LOWER=${GITHUB_REPOSITORY,,}" >> $GITHUB_ENV

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ env.REPO_LOWER }}:${{ github.sha }}
            ghcr.io/${{ env.REPO_LOWER }}:latest

      - name: Trigger staging deployment
        if: ${{ secrets.STAGING_DEPLOY_WEBHOOK_URL != '' }}
        env:
          DEPLOY_WEBHOOK_URL: ${{ secrets.STAGING_DEPLOY_WEBHOOK_URL }}
        run: curl --fail --silent --show-error -X POST "$DEPLOY_WEBHOOK_URL"